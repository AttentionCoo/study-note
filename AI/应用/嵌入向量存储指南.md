# 嵌入向量存储指南：从概念到运维

> 理解 Embedding 与向量库的工作机制，是构建稳定 RAG 系统的前提。本文从 Embedding 的基本概念出发，深入剖析维度冲突的根因，并给出完整的修复方案与日常运维指南。

---

## 目录

- [第一章：背景概念](#第一章背景概念)
- [第二章：项目中的两套向量库](#第二章项目中的两套向量库)
- [第三章：Embedding 模型与维度](#第三章embedding-模型与维度)
- [第四章：维度不匹配的根因分析](#第四章维度不匹配的根因分析)
- [第五章：修复方案](#第五章修复方案)
- [第六章：日常运维](#第六章日常运维)

---

## 第一章：背景概念

### 1.1 Embedding（文本向量化）

Embedding 是将文本转换为固定长度浮点数数组（向量）的过程。它的核心特性是：**语义相近的文本，在向量空间中距离也相近**。

```
"脑卒中急性期的溶栓治疗" → [0.023, -0.451, 0.789, ..., 0.312]  （1024 或 2560 个 float）
```

### 1.2 维度（Dimension）

向量数组的长度，由 Embedding 模型决定。**不同模型产出的向量维度不同，无法混用**：

| 模型 | 维度 | 来源 |
|------|------|------|
| 讯飞官方 Embedding API | 2560 | 云端 HTTP 接口 |
| BAAI/bge-large-zh-v1.5 | 1024 | 本地 CPU 推理 |
| all-MiniLM-L6-v2（ONNX） | 384 | ChromaDB 内置默认模型 |

### 1.3 ChromaDB 向量库

ChromaDB 是一个轻量级向量数据库，语义检索的本质是：将用户查询转为向量，在库中找到距离最近的 K 个文档向量。

**关键约束**：一个 ChromaDB 集合（Collection）创建后，其维度就固定了。后续写入的任何向量必须与创建时的维度一致，否则会直接报错：

```
Embedding dimension 1024 does not match collection dimensionality 2560
```

### 1.4 为什么维度会变

本项目的 Embedding 类 `XfyunEmbeddings` 封装了**自动降级**逻辑：

```
                 ┌─ 讯飞 API 正常 ──→ 2560d
                 │
XfyunEmbeddings ─┤
                 │                 ┌─ 成功 → BGE 1024d
                 └─ 讯飞 API 失败 ──┤
                                   └─ 失败 → ChromaDB 默认 ONNX 384d
```

一旦降级触发，产出的向量维度就变了，与旧数据冲突。

---

## 第二章：项目中的两套向量库

本项目有 **两个独立的 ChromaDB 持久化集合**，用途不同但共享同一个 Embedding 类：

### 2.1 主知识库（RAG 文档检索）

| 属性 | 值 |
|------|-----|
| 代码位置 | `model/app/rag/retrievers.py` → `build_or_load_vectorstore()` |
| 持久化路径 | `model/data/vector_stores/chroma_db_unified` |
| 数据来源 | `model/data/documents/` 下的 PDF 文档 |
| 检索方式 | 三阶漏斗：向量检索 + BM25 → RRF 融合 → Reranker 精排 |
| Embedding 类 | `XfyunEmbeddings` |

**创建方式**（正确做法，显式传了 embedding_function）：

```python
# retrievers.py
vectordb = Chroma(
    persist_directory=persist_dir,
    embedding_function=embedding_function,  # 显式传入，维度可控
    ...
)
```

### 2.2 共享记忆库（Agent 知识沉淀）

| 属性 | 值 |
|------|-----|
| 代码位置 | `model/app/agents/core/shared_memory.py` → `SharedMemoryStore` |
| 持久化路径 | `model/data/vector_stores/chroma_db_shared_memory` |
| 数据来源 | 多 Agent 对话中由 MetaMemoryFilter 筛选出的高价值知识 |
| 检索方式 | 语义向量检索 |
| Embedding 类 | `_ChromaEmbeddingFunction`（包装 XfyunEmbeddings） |

---

## 第三章：Embedding 模型与维度

### 3.1 三种模型的对比

| 对比维度 | 讯飞云端 API | BGE-large-zh-v1.5 | ONNX 默认 |
|----------|-------------|-------------------|-----------|
| 向量维度 | 2560 | 1024 | 384 |
| 运行位置 | 云端 | 本地 CPU | 本地 CPU |
| 中文语义质量 | 高 | 高（中文专项优化） | 中 |
| QPS 限制 | ≈2（免费档） | 无限制 | 无限制 |
| 延迟 | 网络 RTT + 节流 | 纯本地推理 | 纯本地推理 |
| 模型大小 | 无本地占用 | ~1.3GB | ~90MB |

### 3.2 推荐策略

**生产环境建议使用 `XFYUN_EMBEDDING_ENABLED=false`，全程使用本地 BGE（1024d）**。理由如下：

1. 免费档 QPS ≈ 2，Agent 多专家并行推理时极易触发限流。
2. 本地推理无网络延迟，稳定性更高。
3. BGE 模型在中文语义任务上表现优异，不逊于讯飞云端 API。

---

## 第四章：维度不匹配的根因分析

### 4.1 问题表现

当 ChromaDB 集合创建时使用了某种维度，而后续写入使用了另一种维度时，会触发如下错误：

```
chromadb.errors.InvalidDimensionException:
Embedding dimension 1024 does not match collection dimensionality 2560
```

### 4.2 典型触发场景

**场景一：首次构建用了讯飞 2560d，后续降级到 BGE 1024d**

```
时间线：
T1: 讯飞 API 正常 → 向量库以 2560d 创建 → 写入 2560d 向量
T2: 讯飞 API 欠费/限流 → 降级到 BGE 1024d → 写入 1024d 向量 → 报错！
```

**场景二：ChromaDB 未传 embedding_function，使用了内置 ONNX 384d**

```python
# 危险写法：ChromaDB 会使用默认 ONNX 模型（384d）
collection = client.get_or_create_collection(name="my_collection")

# 安全写法：显式传入 embedding_function
collection = client.get_or_create_collection(
    name="my_collection",
    embedding_function=my_embedding_function  # 维度可控
)
```

---

## 第五章：修复方案

### 5.1 核心修复：`_ChromaEmbeddingFunction` 包装器

**问题**：ChromaDB 在未传入 `embedding_function` 时会使用默认 ONNX 模型（384d），与讯飞 Embedding（2560d）或 BGE（1024d）维度冲突，导致写入/检索全量失败。

**解决方案**：创建 `_ChromaEmbeddingFunction` 包装器，将 `XfyunEmbeddings` 适配为 ChromaDB 要求的接口。

```python
class _ChromaEmbeddingFunction:
    """将 XfyunEmbeddings 包装为 ChromaDB 兼容的 EmbeddingFunction。

    此包装器确保 ChromaDB 集合的维度与 XfyunEmbeddings 当前有效模型一致，
    同时保证降级路径（add 时不传 embeddings）也能产出正确维度的向量。
    """

    def __init__(self, xfyun_embeddings):
        self._xfyun = xfyun_embeddings

    def __call__(self, texts):
        """ChromaDB 要求: (List[str]) -> List[List[float]]"""
        if isinstance(texts, str):
            return [self._xfyun.embed_query(texts)]
        return self._xfyun.embed_documents(texts)
```

### 5.2 显式传入 embedding_function

```python
# 修复前（危险）
self._collection = client.get_or_create_collection(
    name=self.COLLECTION_NAME,
    metadata={"hnsw:space": "cosine"},
)

# 修复后（安全）
self._collection = client.get_or_create_collection(
    name=self.COLLECTION_NAME,
    embedding_function=self._ef,        # ← 显式传入包装器
    metadata={"hnsw:space": "cosine"},
)
```

### 5.3 降级存储路径优化

维度冲突时跳过无效重试，直接给出修复指引：

```python
if "dimension" in err_msg.lower() or "dimensionality" in err_msg.lower():
    logger.error(
        f"[shared_memory] 向量维度冲突，无法存储: {err_msg}\n"
        f"   原因：ChromaDB 集合维度与当前 embedding 模型输出维度不一致。\n"
        f"   解决：删除向量库目录后重启服务，统一使用一种 embedding 模型。\n"
        f"   向量库路径: {self.persist_dir}\n"
        f"   建议：在 .env 中设置 XFYUN_EMBEDDING_ENABLED=false 全程使用本地 BGE（1024d）"
    )
    return None  # 直接返回，不浪费重试
```

### 5.4 配置文件修改

在 `.env` 中新增配置项，主动关闭讯飞 Embedding，避开 QPS=2 限制：

```bash
# 文本向量化（embedding）模式
# false = 全程使用本地 BGE-large-zh-v1.5（1024d），无需讯飞额度，无 QPS 限制
# true  = 优先使用讯飞云端 embedding（2560d），失败时自动降级 BGE
# 讯飞免费档 QPS≈2，高并发下极易触发限流 → 建议 false
XFYUN_EMBEDDING_ENABLED=false
```

### 5.5 部署前必须执行的步骤

旧的向量库可能已被讯飞 2560d "污染"，必须删除后重建：

```bash
# 在服务器上执行（路径相对于 model/ 目录）
rm -rf model/data/vector_stores/chroma_db_unified
rm -rf model/data/vector_stores/chroma_db_shared_memory
```

重启服务后，系统会自动：
1. 加载 BGE-large-zh-v1.5 模型（首次需下载 ~1.3GB）
2. 用 1024d 统一维度创建/重建两个 ChromaDB 集合
3. 重新从 PDF 文档构建主知识库向量
4. 共享记忆库从零开始积累

---

## 第六章：日常运维

### 6.1 健康检查

```bash
# 检查向量库状态
ls -la model/data/vector_stores/chroma_db_unified/
ls -la model/data/vector_stores/chroma_db_shared_memory/

# 检查日志中的 embedding 初始化信息
grep "embedding_function" logs/app.log
# 预期看到: embedding_function=_ChromaEmbeddingFunction

grep "BGE 兜底" logs/app.log
# 如果 XFYUN_EMBEDDING_ENABLED=false，应该看到:
# "XFYUN_EMBEDDING_ENABLED=false，跳过讯飞 embedding，直接使用本地 BGE"
```

### 6.2 重建向量库

当文档更新后，需要重建主知识库：

```bash
cd model
python scripts/build_vectorstore.py
```

### 6.3 切换回讯飞 Embedding

如果后续升级了讯飞 Embedding 额度（QPS 提升），可以切回：

```bash
# .env 中修改
XFYUN_EMBEDDING_ENABLED=true

# 必须删除旧库重建（1024d → 2560d 维度变了）
rm -rf model/data/vector_stores/chroma_db_unified
rm -rf model/data/vector_stores/chroma_db_shared_memory

# 重启服务
```

### 6.4 维度冲突排查口诀

```
日志出现 "dimension" + "does not match" → 三件事：
1. 确认当前使用的 embedding 模型（grep XFYUN_EMBEDDING_ENABLED .env）
2. 删除两个向量库目录
3. 重启服务，让它用统一维度重建
```

---

> **文档版本**：v2.0
> **最后更新**：2026-07-15
> **适用项目**：LearnAgent — 基于大模型的个性化资源生成与学习多智能体系统
